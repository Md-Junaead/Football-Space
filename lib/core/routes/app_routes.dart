// lib/core/routes/app_routes.dart
//
// Only Live Score related routes are active right now. Other features
// (auth, quiz play, country picker, favorites, profile) are disabled,
// so their route names have been removed to keep this list accurate.
// ══════════════════════════════════════════════════════════════
class AppRoutes {
  AppRoutes._();

  static const String shell = '/';
  static const String matchDetail = '/match-detail';
}
